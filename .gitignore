# Personal data files - NEVER commit these
*.personal.gs
*.personal.js
*-personal.*
my-*.gs
private/
personal/
backups/

# Reorg toolkit configuration — personal sender data (friend/family addresses,
# per-sender filing rules, thread IDs, label taxonomy). Kept local only, like the
# ORGANIZATION_PLAN. The logic that consumes it lives in reorg_toolkit.gs, which
# IS committed.
_private_data.gs

# Superseded by the reorg_toolkit.gs / _private_data.gs split (2026-08-26).
_find_overlaps.gs
_find_overlaps.js

# Data exports
*.csv
*.xlsx
*.xls
*.json
!package.json
!package-lock.json
!appsscript.json

# Logs
*.log
logs/

# IDE and editor files
.vscode/
.idea/
*.swp
*.swo
*~
*.sublime-project
*.sublime-workspace

# OS files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db
ehthumbs.db
Desktop.ini

# Node.js (if using clasp or other tools)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# clasp (Google Apps Script CLI) - script ID is deployment-specific, not committed
.clasp.json
.clasprc.json
# appsscript.json is the project manifest and IS tracked

# Environment files
.env
.env.local
.env.*.local
*.env

# Build outputs
dist/
build/
out/

# Test coverage
coverage/
.nyc_output/

# Temporary files
tmp/
temp/
*.tmp
*.temp

# Internal Claude Code prompt guides
pages_helper.md
